[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "kv-capacity-estimator"
version = "0.2.0"
description = "Replay request traces and estimate KV-cache capacity with exact LRU stack distances."
readme = { file = "README.zh-CN.md", content-type = "text/markdown" }
requires-python = ">=3.10"
license = { file = "LICENSE" }
classifiers = [
    "Development Status :: 3 - Alpha",
    "License :: OSI Approved :: Apache Software License",
    "Operating System :: OS Independent",
    "Programming Language :: Python :: 3",
    "Programming Language :: Python :: 3 :: Only",
    "Topic :: System :: Benchmark",
]
dependencies = []

[project.urls]
Repository = "https://github.com/llc-kc/kv_capacity_estimator"

[project.scripts]
kv-capacity-token-replay = "token_ids_replay:main"
kv-capacity-request-replay = "request_replay:main"
kv-capacity-openai-to-token-ids = "openai_to_token_ids:main"

[tool.setuptools]
package-dir = { "" = "src" }
py-modules = ["token_ids_replay", "request_replay", "openai_to_token_ids"]

[tool.setuptools.packages.find]
where = ["src"]
